/**
 * @jsx React.DOM
 */
define(['react/addons'], function (React) {
    return React.createClass({
        render: function () {
            return (
                <h1>AHOY!</h1>
            );
        }
    });
});
